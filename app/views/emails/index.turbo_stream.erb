<%= turbo_stream_action_tag(
      "append",
      target: "emails",
      template: %(#{render @emails})
    ) %>
<%= turbo_stream_action_tag(
      "replace",
      target: "pager",
      template: %(#{render "pager", pagy: @pagy})
    ) %>
